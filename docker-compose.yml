services:
  web:
    volumes:
      - ./app2:/app2
    build: .
    env_file:
      - .env
      - sec_key.env
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000
    restart: always